This RTAEBSim application loads a packet from file and send it to the RTAReceiver using the Ice RTAReceiver interface.

To build/instal run:

  $ make
  $ make install

To run you need to provide the the raw data file.

  $ ./RTAEBSim --Ice.Config=config.sim file.raw 0
